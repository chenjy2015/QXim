<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <!--<item name="android:windowBackground">@color/colorAccent</item>-->
        <!--<item name="android:windowFullscreen">true</item>-->
        <!--<item name="windowNoTitle">true</item>-->
    </style>

    <style name="Cus" />

    <style name="Cus.TabRadioButton2">
        <item name="android:layout_width">@dimen/dp_10</item>
        <item name="android:layout_height">@dimen/dp_10</item>
        <item name="android:layout_margin">@dimen/dp_10</item>
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/selector_guide_indicator</item>
    </style>

    <!-- 应用启动页(StartingWindow)的theme -->
    <style name="AppTheme.StartingWindowTheme" parent="AppTheme">
        <!-- 可以设置成纯颜色（设置一个和Activity UI相似的背景） -->
        <item name="android:windowBackground">@color/colorAccent</item>
        <!--也可以设置成一张图片 -->
        <!--<item name="android:windowBackground">@drawable/shape_red_rect</item>-->
    </style>
</resources>
